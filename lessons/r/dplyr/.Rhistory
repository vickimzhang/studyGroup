# URL of the dataset on Figshare
file_url <- "https://ndownloader.figshare.com/files/2292169"
# Destination file path on your local machine
destination_file <- "portal_data_joined.csv"
# Download the file
download.file(file_url, destination_file)
# Load the data into a data frame (using readr package for read_csv)
library(readr)
survey_data <- read_csv(destination_file)
?pivot_wider
# Chunk 1: setup
knitr::opts_chunk$set(eval = FALSE)
# Chunk 2
# URL of the dataset on Figshare
file_url <- "https://ndownloader.figshare.com/files/2292169"
# Destination file path on your local machine
destination_file <- "portal_data_joined.csv"
# Download the file
download.file(file_url, destination_file)
# Load the data into a data frame (using readr package for read_csv)
library(readr)
survey_data <- read_csv(destination_file)
# Chunk 3
str(surveys)
surveys_gw
surveys <- read_csv(destination_file)
# Chunk 1: setup
knitr::opts_chunk$set(eval = FALSE)
# Chunk 2
# URL of the dataset on Figshare
file_url <- "https://ndownloader.figshare.com/files/2292169"
# Destination file path on your local machine
destination_file <- "portal_data_joined.csv"
# Download the file
download.file(file_url, destination_file)
# Load the data into a data frame (using readr package for read_csv)
library(readr)
surveys <- read_csv(destination_file)
# Chunk 3
str(surveys)
head(surveys)
# Chunk 4
select(surveys, plot_id, species_id, weight, year)
library(tidyverse)
# Chunk 3
library(tidyverse)
str(surveys)
head(surveys)
# Chunk 4
select(surveys, plot_id, species_id, weight, year)
# Chunk 5
filter(surveys, year == 1995)
# Chunk 6
2 < 3
5 > 9
5 == 5
# Chunk 7
2 != 3 # Not equal
2 <= 3 # Less than or equal to
5 >= 9 # Greater than or equal to
# Chunk 8
2 %in% c(2, 3, 4) # Checks whether 2 is in c(2, 3, 4), returns logical vector
# Chunk 9
temp_df <- select(surveys, plot_id, species_id, weight, year)
filter(temp_df, year == 1995)
# Chunk 10
filter(select(surveys, plot_id, species_id, weight, year), year == 1995)
# Chunk 11
surveys %>%
select(., plot_id, species_id, weight, year) %>%
filter(., year == 1995)
# Chunk 12
surveys %>%
select(plot_id, species_id, weight, year) %>%
filter(year == 1995)
# Chunk 13
surveys %>%
select(plot_id, species_id, weight, year) %>%
filter(year == 1995) %>%
head()
# Chunk 14
surveys_1995 <- surveys %>%
select(plot_id, species_id, weight, year) %>%
filter(year == 1995)
surveys_1995
# Chunk 15
surveys %>%
filter(weight < 10) %>%
select(species_id, sex, weight)
# Chunk 16
surveys %>%
filter(taxa == 'Rodent' & sex == 'F') %>%
select(sex, taxa)
# Chunk 17
surveys %>%
filter(species == 'clarki' | species == 'audubonii') %>%
select(species, taxa)
# Chunk 18
surveys %>%
mutate(weight_kg = weight / 1000)
# Chunk 19
surveys %>%
mutate(weight_kg = weight / 1000,
weight_kg2 = weight_kg * 2)
# Chunk 20
surveys %>%
filter(!is.na(weight)) %>%
mutate(weight_kg = weight / 1000)
# Chunk 21
## Answer
surveys_hindfoot_half <- surveys %>%
filter(!is.na(hindfoot_length)) %>%
mutate(hindfoot_half = hindfoot_length / 2) %>%
filter(hindfoot_half < 30) %>%
select(species_id, hindfoot_half)
# Chunk 22
surveys %>%
group_by(sex) %>%
summarize(mean_weight = mean(weight))
# Chunk 23
surveys %>%
filter(!is.na(weight)) %>%
group_by(sex) %>%
summarize(mean_weight = mean(weight))
# Chunk 24
surveys %>%
filter(!is.na(weight) & !is.na(sex)) %>%
group_by(sex) %>%
summarize(mean_weight = mean(weight))
# Chunk 25
surveys %>%
filter(!is.na(weight) & !is.na(sex)) %>%
group_by(genus, sex) %>%
summarize(mean_weight = mean(weight))
# Chunk 26
filtered_surveys <- surveys %>%
filter(!is.na(weight) & !is.na(sex)) %>%
group_by(genus, sex)
# Chunk 27
filtered_surveys %>%
summarize(mean_weight = mean(weight),
min_weight = min(weight))
# Chunk 28
# Answer 1
surveys %>%
filter(!is.na(hindfoot_length)) %>%
group_by(species) %>%
summarize(mean_hindfoot_length = mean(hindfoot_length)) # 31
# Answer 2
surveys %>%
filter(!is.na(weight)) %>%
filter(year == 1979) %>%
filter(weight == max(weight)) %>%
select(genus, species) # Neotoma albigula (white-throated woodrat)
# Chunk 29
surveys %>%
group_by(taxa) %>%
tally()
# Chunk 30
surveys %>%
group_by(taxa, sex) %>%
tally()
# Chunk 31
surveys %>%
group_by(species) %>%
tally()
# Chunk 32
surveys %>%
group_by(species) %>%
tally() %>%
arrange(n) # `n` is the name of the column `tally` generated
# Chunk 33
surveys %>%
group_by(species) %>%
tally() %>%
arrange(desc(n)) %>%
head(5)
# Chunk 34
surveys %>%
group_by(species, taxa, genus) %>%
tally() %>%
arrange(desc(n)) %>%
head(5)
# Chunk 35
surveys %>%
group_by(plot_type) %>%
tally()
# Chunk 36
surveys %>%
group_by(sex) %>%
summarize(n = n())
# Chunk 37
surveys_gw <- surveys %>%
filter(!is.na(weight)) %>%
group_by(genus, plot_id) %>%
summarize(mean_weight = mean(weight))
head(surveys_gw)
# Chunk 38
surveys_gw_wide <- surveys_gw %>%
spread(genus, mean_weight)
head(surveys_gw_wide)
# Chunk 39
surveys_gw %>%
spread(genus, mean_weight, fill = 0) %>%
head()
# Chunk 40
surveys_gw_long <- surveys_gw_wide %>%
gather(genus, mean_weight, -plot_id)
head(surveys_gw_long)
# Chunk 41
surveys_gw_wide %>%
gather(genus, mean_weight, Baiomys:Onychomys) %>%
head()
# Chunk 42
surveys_gw_wide %>%
gather(genus, mean_weight, -plot_id, -Perognathus) %>%
filter(!is.na(mean_weight)) %>%
head()
surveys_gw
?pivot_wider
surveys_gw_wide <- surveys_gw %>%
pivot_wider(genus, mean_weight)
surveys_gw
surveys_gw_wide <- surveys_gw %>%
pivot_wider(names_from = genus, values_from = mean_weight)
head(surveys_gw_wide)
surveys_gw
surveys_gw %>%
spread(genus, mean_weight, values_fill = 0) %>%
head()
surveys_gw %>%
pivot_wider(names_from = genus, values_from = mean_weight,
values_fill = 0) %>%
head()
surveys_gw_wide
?pivot_longer
surveys_gw_long <- surveys_gw_wide %>%
pivot_longer(names_to = genus, values_to = mean_weight, cols = -plot_id)
surveys_gw_long <- surveys_gw_wide %>%
pivot_longer(names_to = "genus", values_to = "mean_weight", cols = -plot_id)
head(surveys_gw_long)
head()
surveys_gw_wide %>%
pivot_longer(names_to = "genus", values_to = "mean_weight", cols = Baiomys:Onychomys)
surveys_gw_wide %>%
pivot_longer(names_to = "genus", values_to = "mean_weight", cols = Baiomys:Onychomys) %>%
head()
surveys_gw_wide %>%
pivot_wider(names_to = "genus", values_to = "mean_weight",
cols = c(-plot_id, -Perognathus)) %>%
filter(!is.na(mean_weight)) %>%
head()
surveys_gw_wide
surveys_gw_wide %>%
pivot_longer(names_to = "genus", values_to = "mean_weight",
cols = c(-plot_id, -Perognathus)) %>%
filter(!is.na(mean_weight)) %>%
head()
